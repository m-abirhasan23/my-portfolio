---
import profile from "../assets/Photo.png";

import span from "../assets/recruitment.png";
import Button from "../components/Button.astro";
import Title from "../components/Title.astro";
const title = {
  text: "about me",
  subtext: "my introduction",
};
---

<section id="about-me" class="about-me" aria-label="About Me">
  <Title {title} />
  <div class="container">
    <div class="about-img">
      <img src={profile} alt="abir pictures " />
    </div>
    <div class="about-info">
      <h3>i’m a <span>frontend developer</span> from chattogram bangladesh</h3>
      <p>
        I'm a front-end developer with strong technical skills and a passion for
        solving customer problems. I use modern frameworks and tools to build
        high-performance, user-friendly products that meet unique customer
        needs. I stay current with industry trends and remain adaptable to
        change. I excel as both an independent worker and a team player,
        delivering exceptional results that drive business growth and enhance
        the user experience.
      </p>
      <Button type="button" text="Checkout My Resume" span={span} />
    </div>
  </div>
</section>

<style>
  .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    width: 90%;
    margin-inline: auto;
    margin-block: 2em;
  }
  .about-img {
    width: 35%;
    position: relative;
    z-index: 2;
  }
  .about-img::after {
    position: absolute;
    width: 100%;
    height: 100%;
    background-color: hsla(var(--clr-primary), 0.6);
    z-index: 5;
    content: "";
    inset: 0;
  }
  .about-img > img {
    width: 100%;
    border: 5px solid hsl(var(--clr-accent), 0.9);
    border-radius: 20px;
    margin-inline: auto;
  }
  .about-info {
    width: 60%;
  }
  .about-info h3 {
    font-size: var(--fs-md);
    font-family: var(--font-secondary);
    text-transform: uppercase;
  }
  .about-info h3 > span {
    color: hsl(var(--clr-accent), 0.8);
    text-decoration: underline;
  }
  .about-info p {
    font-family: var(--font-primary);
    margin-block: 1em;
    font-size: var(--fs-regular);
  }

  @media (max-width: 600px) {
    .container {
      width: 90%;
      margin-inline: auto;
      justify-content: center;
      gap: 4rem;
    }
    .about-img {
      width: 100%;
    }
    .about-img > img {
      width: 90%;
    }
    .about-info {
      width: 100%;
    }
    .about-info > p {
      font-size: var(--fs-md);
    }
  }
</style>
