---
// import portfolioImg from "../assets/portfolio.avif";
---

<section class="hero" aria-label="Hero Section" id="hero">
  <div class="container">
    <h1 class="headline">who is<span class="name">abir hasan</span></h1>
    <div class="subtitle">
      <p class="job-title">
        A front end developer & designer from Chattogram, Bangladesh.
      </p>
      <span>
        I like to build scalable frontend products with great user experiences
        with some the best modern web frameworks</span
      >
    </div>
    <button>Say Hello 👋</button>
  </div>
</section>

<style>
  .hero {
    width: 100%;
    display: grid;
    place-items: center;
    min-height: 100vh;
    text-align: center;
  }
  .container {
    width: 90%;
    margin-inline: auto;
  }
  .headline {
    font-size: 156px;
    text-transform: uppercase;
    font-weight: 800;
    -webkit-text-stroke: 3.5px hsl(var(--clr-accent));
    color: transparent;
    line-height: 100%;
  }
  .name {
    display: block;
    width: 100%;
    color: hsl(var(--clr-accent));
  }
  .subtitle {
    font-size: var(--fs-regular);
    text-transform: capitalize;
    width: 70%;
    margin-block: 1rem;
    margin-inline: auto;
  }
  .job-title {
    font-size: var(--fs-md);
    text-transform: capitalize;
    display: inline-block;
    font-weight: 500;
    color: hsla(var(--clr-accent), 1);
    display: block;
  }
  button {
    background: hsl(var(--clr-accent));
    border: 1px solid hsl(var(--clr-accent));
    font-family: var(--font-secondary) !important;
    text-transform: uppercase;
    font-weight: 700;
    height: 8vh;

    font-size: var(--fs-md);
    font-weight: 700 !important;
    color: hsl(var(--clr-secondary));
    border-radius: 5px;
    transition: background 400ms cubic-bezier(0, 0.2, 0.23, 1.07);
    text-align: center;

    min-width: 18vw;
    /* padding: 11px; */
    cursor: pointer;
  }
  button:hover {
    background-color: hsla(var(--clr-accent), 0.7);
  }

  @media (max-width: 600px) {
    .container {
      width: 90%;
      text-align: center;
    }
    .headline {
      font-size: 23vw;
      text-align: center;
    }
    .subtitle {
      width: 100%;
      font-size: var(--fs-md);
      text-align: center;
    }
    .job-title {
      display: inline;
      font-size: var(--fs-md);
    }
    button {
      width: 55vw;
      font-size: var(--fs-md);
      font-weight: 400;
      padding: 0;
    }
  }
</style>
